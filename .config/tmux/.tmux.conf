# terminal color
set -g default-terminal "screen-256color"

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# enable mouse
set -g mouse on

# allows for fast key repetition
set -s escape-time 5
